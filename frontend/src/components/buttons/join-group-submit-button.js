import './join-group-submit-button.css';


const JoinGroupSubmitButton = ({onClickFunction}) => {

    return (
    <div className='join_group_submit_button' onClick={onClickFunction}>JOIN GROUP</div>
    );
}
export default JoinGroupSubmitButton;